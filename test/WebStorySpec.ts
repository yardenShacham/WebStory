import {expect} from 'chai';
import {WebStory} from '../src/webStory';

describe('WebStory Test', () => {

  describe('web story init test', () => {

    it('should pass', () => {

      expect(true).to.be.equal(true);
    });

  });

});
